export type LocData = {
  city: string;
  region: string;
  coordinates: string;
};

export type WeatherAlert = {
  areaDesc: string;
  event: string;
  severity: string;
  status: string;
  headline: string;
  description: string;
  effective: string;
  ends: string;
  instruction: string;
};
